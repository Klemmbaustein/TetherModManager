
const backgroundColor = (0.05, 0.05, 0.15);

element MainElement
{
	var Message;

	position = (-1, -1);

	child UIBackground
	{
		size = (2, 2)sr;

		color = backgroundColor;

		allAlign = centered;

		child UIText
		{
			text = Message;
			size = 40px;
			color = 1;
		}
	}
}
